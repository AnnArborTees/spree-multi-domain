<tr>
  <td><label><%=t("store")%></label></td>
  <td>
    <%= collection_select(:tracker, :store_id, [Struct.new(:id, :name).new(nil, 'All')] + Spree::Store.all, :id, :name, {}, { class: 'select2 fullwidth' }) %>
  </td>
</tr>
